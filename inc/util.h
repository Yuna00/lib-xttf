#ifndef UTIL_H_
#define UTIL_H_

#include <fcntl.h>
#include <stdint.h>
#include <unistd.h>

// inline uint16_t getu16(int fd, uint32_t offset);
// inline uint32_t getu32(int fd, uint32_t offset);
#endif /* UTIL_H_ */
